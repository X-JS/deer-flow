[project]
name = "deerflow-quota-extension"
version = "0.1.0"
description = "Enforce a per-run tool-call quota as a DeerFlow Python extension"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "deerflow-extension-api>=0.2,<0.3",
    "fastapi>=0.115.0,<1",
    "langchain>=1.3,<2",
]

[project.entry-points."deerflow.extensions"]
quota = "deerflow_quota_extension:install"

[project.optional-dependencies]
dev = [
    "httpx>=0.28,<1",
    "pytest>=9,<10",
    "ruff>=0.14,<1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["deerflow_quota_extension"]

[tool.ruff]
line-length = 240
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.ruff.lint.isort]
known-first-party = ["deerflow_quota_extension"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
